#include <bits/stdc++.h>

using namespace std;

int main(){
    /* int n, temp, temp2, temp4 = 1, pivot = 0, temp5 = 0;
    cin >> n;
    int arr[n], temp3[n];
    
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    
    for (int i = 0; i < n; i++) {
        for (int j = i + 1; j < n; j++) {
            if (arr[i] > arr[j]) {
                temp = arr[i];
                arr[i] = arr[j];
                arr[j] = temp;
            }
        }
    }
    
    temp2 = arr[0];
    for (int i = 0; i < n; i++) {
        if (abs(temp2 - arr[i + 1]) == 0) {
            temp4++;
        } else {
            temp3[pivot] = temp4;
            temp4 = 1;
            pivot++;
            temp2 = arr[i + 1];
        }
    }
    
    for (int i = 0; i < n; i++) {
        if (temp3[i] > 100 || temp3[i] == 0) {
            break;
        }
        if (temp5 < temp3[i]) {
            temp5 = temp3[i];
        }
    }

    cout << n - temp5; */

    cout << a << "\n";
    
    return 0;
}